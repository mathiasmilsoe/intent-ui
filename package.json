{
  "name": "intent-ui",
  "version": "0.0.1",
  "description": "Intent-driven UI for the AI era: dynamic layouts, components and state based on user intent.",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
"scripts": {
  "build": "rm -rf dist && tsc -p tsconfig.json && cp -R src/styles dist",
  "clean": "rm -rf dist",
  "typecheck": "tsc -p tsconfig.json --noEmit"
},
  "keywords": [
    "react",
    "ui",
    "intent",
    "ai",
    "llm",
    "dashboard",
    "layout"
  ],
  "author": "Mathias Milsøe",
  "license": "MIT",
  "peerDependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.6.0"
  }
}